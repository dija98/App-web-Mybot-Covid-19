************** English *****************

Hello,
you will find here folders each containing python codes, they share the same json file (intents.json)
the "python code" folder if you want to run the code in the terminal
the "python and Flask" folder if you want to run the code on localhost

for "python code" requirements:
    python=3.6
    pip install numpy,nltk, tflearn and tensorflow=1.5
   

for "python and Flask code" requirements:
    install previous packages + Flask, flask socketio and eventlet

that's all :)

************** French  *****************

Bonjour,
vous trouverez deux dossiers contenant chacun les codes de python, ils partagent le même fichier json (intents.json)
le dossier "code python" si vous voulez exécuter le code dans le terminal
le dosssier "python et Flask" si vous voulez exécuter le code  sur localhost

pour "code python" installer:
   python =3.6
   pip install numpy,nltk, tflearn et tensorflow=1.5
   

pour "code python et Flask":
   installer les packages précédents + Flask, flask socketio et eventlet 

c'est tout :)


